# Web Framework & Server
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Database & ORM
sqlmodel>=0.0.22
SQLAlchemy>=2.0.0
psycopg2-binary>=2.9.9
supabase>=2.0.0

# Authentication & Security (เปลี่ยนมาใช้ PyJWT)
pyjwt>=2.8.0
cryptography>=42.0.0

# HTTP Client (สำหรับเรียก API ภายนอก)
requests>=2.31.0

# Environment & Utilities
python-dotenv>=1.0.1
python-multipart>=0.0.9

# AI Detection (app/services/ai_inference.py — Dentex model, Feature 5)
# CPU build by default. For an NVIDIA GPU machine, install the CUDA build
# from https://pytorch.org instead before installing the rest of this file.
torch>=2.2.0
torchvision>=0.17.0
ultralytics>=8.2.0
numpy>=1.26.0
pillow>=10.2.0